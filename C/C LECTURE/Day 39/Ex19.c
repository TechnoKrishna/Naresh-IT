#include <stdio.h>
#include <conio.h>
#include <math.h>

int main() {

    // Find the absolute[Always +Ve] value of given no?

    int n;

    printf("Enter A No : ");
    scanf("%d", &n);

    printf("Absolute Value = %d", abs(n));
    
    return 0;
}