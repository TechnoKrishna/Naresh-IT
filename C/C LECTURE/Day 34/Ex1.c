#include <stdio.h>
#include <conio.h>

int main() {

    int a, b, c;

    printf("Enter Two No's : ");
    c = scanf("%d%d", &a, &b);
    printf("a = %d, b = %d conversions = %d", a, b, c);

    return 0;
}