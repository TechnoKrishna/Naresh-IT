#include <stdio.h>
#include <conio.h>

int main() {

if ("false") puts("Yes"); else puts ("No");
printf("abc addr %u, ab addr %u\n", "abc", "ab");
if("abc" > "ab") puts("Yes"); else puts ("No");

    return 0;
}