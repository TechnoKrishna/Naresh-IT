#include <stdio.h>
#include <conio.h>

// GLOBAL VARIABLES

float a;
int b;
char c;

int main() {

    // LOCAL VARIABLES

    float x;
    int y;
    char z;

    printf("a = %f, b = %d, c = %c\n", a, b, c);
    printf("x = %f, y = %d,  = %c\n", x, y, z);

    return 0;
}

