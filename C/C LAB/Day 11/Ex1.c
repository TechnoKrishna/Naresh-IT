#include <stdio.h>

int main()
{
    int age;
    scanf("%d", &age);

    if(age > 21)
    printf("you are eligible to marriage.");
    else
    printf("Sorry are not eligible to marriage.");
    return 0;
}